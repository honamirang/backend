[package]
name = "backend"
version = "0.0.1"
edition = "2024"

[dependencies]
tokio = { version = "1.51.0", features = ["rt-multi-thread"] }
axum = { version = "0.8.8" }
tower-http = { version = "0.6.8", features = ["cors"] }